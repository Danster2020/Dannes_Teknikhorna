import * as React from "react"
import { Link } from "gatsby"

const Header = ({ siteTitle }) => (
  <header>
    <div className="flex justify-end text-2xl mr-10 mt-10 text-white">
      <div>Hem</div>
    </div>
  </header>
)

export default Header
